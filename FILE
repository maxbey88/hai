#include <stdio.h>

int main(void)
{
	int i;
	FILE *file;
	file = fopen("before_sort.txt","r");
	while((chr = fgetc(fp)) != EOF) {
	    putchar(chr); 
	    
	}
	fclose(file);
	printf("i = %d",i);
	return 0;
}
