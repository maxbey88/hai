#include <stdio.h>

int main(void)
{
    char chr;
	int i;
	FILE *fp;
	fp = open ("before_sort.txt","r");
	while((chr = fgetc(fp)) != EOF) {
	    putchar(chr); 
	    
	}
	fclose(file);
	printf("i = %d",i);
	return 0;
}
